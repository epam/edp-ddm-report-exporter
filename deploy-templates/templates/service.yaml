apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-exporter.fullname" . }}
  labels:
    {{- include "report-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "report-exporter.selectorLabels" . | nindent 4 }}
