apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "report-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "report-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "report-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
